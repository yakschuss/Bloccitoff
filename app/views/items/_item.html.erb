    <%= content_tag :div, class: 'media', id: "item-#{item.id}" do %>
        <div class="delete-item col-lg-1">

            <%= link_to "", user_item_path({user_id: @user.id, id: item.id}), method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %>
        </div>
       <li class="list-group-item col-lg-11">
           <div class="created-at-time badge col-lg-3">
                <%= days_left(item) %> left
           </div>

           <div class="item-name col-lg-2">
               <%= item.name %>
           </div>

        </li>
    <% end %>
